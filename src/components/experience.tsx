import * as React from 'react';

export interface IExperienceProps {
}

export default class Experience extends React.Component<IExperienceProps> {
  public render() {
    return (
      <div>
        Experience
      </div>
    );
  }
}
